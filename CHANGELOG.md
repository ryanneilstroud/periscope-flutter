# Changelog

## 0.5.5

- Synced bridge and platform dependency versions to `0.5.5` for parity with `PeriscopeKit`.
- No API surface change: `capture`, `stop`, and `sendTestRequest` remain the supported Flutter APIs.

## 0.1.0

- Initial Flutter bridge for Periscope iOS/Android SDKs.
- Added `capture`, `stop`, and `sendTestRequest` APIs.
